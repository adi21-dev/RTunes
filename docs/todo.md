1) 
2) 
3) u can use controls only when caps is off
4) if audio device changed - improve error show and change the audio output
5) does shift+M do anything - if not remove it
6) visulizer changes - 1) when in the third biggest tab view, it shows two visulizer on the opposite side, change it to 
7) doenloaded but output path not detected - auto rescan